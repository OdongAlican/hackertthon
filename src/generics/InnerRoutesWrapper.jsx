/* eslint-disable  react/prop-types */

import React from 'react';

const InnerRoutesWrapper = ({ children }) => (
  <div className="inner-routes-wrapper">
    {children}
  </div>
);

export default InnerRoutesWrapper;
